import 'package:flutter/material.dart';

spaceHeight(count) {
  return SizedBox(height: double.parse(count.toString()));
}

spaceWidth(count) {
  return SizedBox(width: double.parse(count.toString()));
}
